# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.11.10
pydantic-settings==2.5.2
python-multipart==0.0.12
httpx==0.27.2
# Database — supports both SQLite (dev) and PostgreSQL/Neon (prod)
sqlalchemy==2.0.35
aiosqlite==0.20.0
asyncpg==0.29.0
psycopg2-binary==2.9.9
# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Image processing — all pre-built wheels, no Fortran compiler needed
numpy==1.26.4
opencv-python-headless==4.10.0.84
PyWavelets==1.7.0
Pillow==10.4.0
pydicom==2.4.4
# scikit-image without scipy (uses pre-built wheel on Python 3.11)
scikit-image==0.22.0
# Metrics (pure-Python fallback for SSIM if skimage unavailable)
imageio==2.36.0
